#include <stdio.h>

int main() {
    // int x; // x เป็นค่าขยะที่อยู่ memory
    // int y = 0;
    // int X; // sensitive case

    // // int x; // ห้ามกำหนดตัวแปรซ้ำใน Scope เดียวกัน

    // {
    //     int x;
    // }

    // Overflow
    // -128, -127,..... 127
    // char x = 129;   
    // printf("%d", x);

    // unsigned char c = 250;
    // printf("%d", c);

    // short i = 0; // == short int
    // long l = 0; // == long int
    // long long ll = 0; 

    // print
    // long c = 'A'; // char c = 65;
    // printf("%ld\n", c);
    // printf("%c\n", c);

    // if (c >= 'A' && c <= 'Z') {

    // }

    // if ('\0') {
    //     puts("True");
    // } else {
    //     puts("False");
    // }   

    // int str1[10] = "ABC";
    // int str2[] = "ABC";
    // int str3[10] = {'A', 'B', 'C', '\0'};

    // int x = 5;
    // long y = 123l;

    // // implicit type conversion -> เล็กไปใหญ่
    // float x = 1; // แปลง 1 ให้เป็น 1.000 (แปลงเป็น float)

    // // explicit type conversion
    // int y = (int) 9.9; // แปลง 9.9 ให้เป็น 9.000 (int) 
    
    // printf("x: %f\n", x);
    // printf("y: %d\n", y);

    // printf("%f\n", 1.0 / 2); // แปลง 2 เป็น 2.0 แล้วค่อยหาร

    // int a = 1;
    // int b = 2;
    // printf("%f", (float) a / b);

    // 1 - 2; // Binary operator
    // -2; // Unary Operator

    // int i = 2;
    // printf("%d\n", i--); // แสดงเลขก่อนลบ และลบไป 1
    // printf("%d\n", i);

    // printf("%d\n", --i); // แสดงเลขที่ลบ 1 แล้ว
    // printf("%d\n", i);
    
    // int x = 5;
    // printf("%s", x == 5 ? "True" : "False");

    // x = 1 + 2

    // int x, y;
    // x = y = 5;

    // printf("%d", 1 + 2 * 3); // 7
}